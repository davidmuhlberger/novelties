<h1>All products</h1>

<p>
  <%= link_to "Revenir à l'accueil", root_path, class: "btn btn-primary" %>
  <%= link_to "Ajouter un nouveau produit", new_product_path, class: "btn btn-success" %>
</p>

<ul>
  <% @products.each do |product| %>
    <li>
      <%= product.name %> - <%= product.url %>
      <%= link_to "voir le produit", product_path(product) %>
      </li>
  <% end %>
</ul>
